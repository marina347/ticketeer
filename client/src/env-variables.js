const { REACT_APP_SERVER_PATH, REACT_APP_GOOGLE_AUDIENCE } = process.env;

const EnvVariables = {
  REACT_APP_SERVER_PATH,
  REACT_APP_GOOGLE_AUDIENCE,
};

console.log(EnvVariables);

export default EnvVariables;
